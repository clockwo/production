import { Page } from '@/widgets/Page';
import styles from './AdminPanelPage.module.scss';

const AdminPanelPage = () => (
    <Page className={styles.AdminPanelPage}>
        ADMIN PAGE
    </Page>
);

export default AdminPanelPage;
